<style>
  .snippet_banner {
    width: 100%;
    height: 400px;
    background-size: cover;
  }
  .snippet_banner.half {
    width: 50%;
  }
</style>
<div class="snippet_banner {%if half %}half{%endif%}" style="background-image:url('{{banner_image_url}}');">
  <div class="title">{{ banner_title }}</div>
  <a href="{{button_href}}" class="button">{{ button_title }}</a>
</div>